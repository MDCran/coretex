# Changelog

All notable changes are documented here. Versions follow SemVer with `alpha`/`beta`/`rc` prereleases.

## Unreleased

- Version metadata is surfaced in the sidebar and `/api/version`; build, security,
  and release validation are managed by the Coretex monorepo's root workflows.
- Career module: AI job search, interview rounds, prep + STAR library, offers & negotiation,
  networking log, analytics, AI tools (ATS / cover letter / skills gap / salary benchmark),
  goals board, and ghosting/follow-up automation.
- Dashboard: bottom-right hover-expand widget controller.
